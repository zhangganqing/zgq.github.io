<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>欢迎!</title>
    <url>/2020/04/24/%E6%AC%A2%E8%BF%8E/</url>
    <content><![CDATA[<h1 id="Welcome"><a href="#Welcome" class="headerlink" title="Welcome"></a>Welcome</h1><p>这里是signskyc - 星座天成 的BLOG</p>
<p>QQ:1693435485</p>
<p>email: <a href="mailto:signskyc@qq.com">signskyc@qq.com</a></p>
<p>慢慢阅读吧！</p>
]]></content>
      <tags>
        <tag>置顶</tag>
      </tags>
  </entry>
  <entry>
    <title>C ++ 游戏集合</title>
    <url>/2020/04/24/C-%E6%B8%B8%E6%88%8F%E9%9B%86%E5%90%88/</url>
    <content><![CDATA[<h1 id="C-游戏整合"><a href="#C-游戏整合" class="headerlink" title="C++ 游戏整合"></a>C++ 游戏整合</h1><p>本次更新加入了6个游戏,该游戏整合里的游戏不是我做的，是我多年搜集出来，方便大家游玩的，全是C++游戏，感兴趣的话,可以加入<a href="https://www.luogu.org/team/show?teamid=14042" target="_blank" rel="noopener">团队</a>,实时更新哦。</p>
<h2 id="有作者需要声明版权请私信我"><a href="#有作者需要声明版权请私信我" class="headerlink" title="有作者需要声明版权请私信我"></a>有作者需要声明版权请私信我</h2><p>下载:</p>
<hr>
<blockquote>
<p><a href="https://share.weiyun.com/5hCmsKS" target="_blank" rel="noopener">腾讯微云</a></p>
</blockquote>
<blockquote>
<p><a href="https://jq.qq.com/?_wv=1027&k=5G4Rxb5" target="_blank" rel="noopener">QQ（下载较快）</a></p>
</blockquote>
<blockquote>
<p><a href="https://pan.baidu.com/s/1Cmg97C42wG9ejZtkuGI5dQ" target="_blank" rel="noopener">百度网盘</a>  提取码：jici</p>
</blockquote>
<hr>
<h3 id="制作环境：WIN-10-64位"><a href="#制作环境：WIN-10-64位" class="headerlink" title="制作环境：WIN 10 64位"></a>制作环境：WIN 10 64位</h3><p>(最近换机了)</p>
<p>抵制不良游戏，拒绝盗版游戏。<br>注意自我保护，谨防受骗上当。<br>适度游戏益脑，沉迷游戏伤身。<br>合理安排时间，享受健康生活。</p>
<h2 id="制作团队"><a href="#制作团队" class="headerlink" title="制作团队"></a><a href="https://www.luogu.org/team/show?teamid=14042" target="_blank" rel="noopener">制作团队</a></h2>]]></content>
      <tags>
        <tag>GAME</tag>
      </tags>
  </entry>
  <entry>
    <title>c++ 常用函数</title>
    <url>/2020/04/24/c-%E5%B8%B8%E7%94%A8%E5%87%BD%E6%95%B0/</url>
    <content><![CDATA[<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#x2F;&#x2F;逐个字输出</span><br><span class="line">void slow(char *p)</span><br><span class="line">     &#123;</span><br><span class="line">         while(1)</span><br><span class="line">         &#123;</span><br><span class="line">             if(*p!&#x3D;0)</span><br><span class="line">                 printf(&quot;%c&quot;,*p++);</span><br><span class="line">             else </span><br><span class="line">                break;</span><br><span class="line">             Sleep(100);</span><br><span class="line">         &#125;</span><br><span class="line">     &#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">ofstream ofile(&quot;1.txt&quot;);&#x2F;&#x2F;写入</span><br><span class="line">ofile&gt;&gt;;</span><br><span class="line">ifstream ifile(&quot;1.txt&quot;);&#x2F;&#x2F;读取</span><br><span class="line">ifile&gt;&gt;;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">srand(time(NULL));&#x2F;&#x2F;种子</span><br><span class="line"></span><br><span class="line">getch();直接读取键盘输入无显示，char</span><br><span class="line"></span><br><span class="line">getche();同上,有显示</span><br><span class="line"></span><br><span class="line">_sleep(); 延迟</span><br><span class="line"></span><br><span class="line">&#x2F;&#x2F;隐藏\显示光标----------------------------</span><br><span class="line">void hidden() &#123;</span><br><span class="line">    HANDLE hOut &#x3D; GetStdHandle(STD_OUTPUT_HANDLE);</span><br><span class="line">    CONSOLE_CURSOR_INFO cci;</span><br><span class="line">    GetConsoleCursorInfo(hOut,&amp;cci);</span><br><span class="line">    cci.bVisible&#x3D;0;&#x2F;&#x2F;赋值1显示光标</span><br><span class="line">    SetConsoleCursorInfo(hOut,&amp;cci);</span><br><span class="line">    return;</span><br><span class="line">&#125; </span><br><span class="line"></span><br><span class="line">void unhidden() &#123;</span><br><span class="line">    HANDLE hOut &#x3D; GetStdHandle(STD_OUTPUT_HANDLE);</span><br><span class="line">    CONSOLE_CURSOR_INFO cci;</span><br><span class="line">    GetConsoleCursorInfo(hOut,&amp;cci);</span><br><span class="line">    cci.bVisible&#x3D;1;&#x2F;&#x2F;赋值1显示光标</span><br><span class="line">    SetConsoleCursorInfo(hOut,&amp;cci);</span><br><span class="line">    return;</span><br><span class="line">&#125; </span><br><span class="line"></span><br><span class="line">鼠标点击</span><br><span class="line"> while(true)</span><br><span class="line">    &#123;</span><br><span class="line">    	if(KEY_DOWN(MOUSE_MOVED))</span><br><span class="line">    	&#123;</span><br><span class="line">    POINT pt;</span><br><span class="line">    HWND hwnd&#x3D;GetForegroundWindow();</span><br><span class="line">    GetCursorPos(&amp;pt);</span><br><span class="line">    ScreenToClient(hwnd,&amp;pt);</span><br><span class="line">    pt.y&#x3D;pt.y&#x2F;8,pt.x&#x3D;pt.x&#x2F;8;</span><br><span class="line">&#x2F;&#x2F;    cout&lt;&lt;pt.y&lt;&lt;&quot; &quot;&lt;&lt;pt.x&lt;&lt;&quot; &quot;;</span><br><span class="line">&#x2F;&#x2F;    _sleep(500);</span><br><span class="line">    if(pt.y&gt;&#x3D;44&amp;&amp;pt.x&gt;&#x3D;43&amp;&amp;pt.y&lt;&#x3D;45&amp;&amp;pt.x&lt;&#x3D;56)</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">    </span><br><span class="line">    </span><br><span class="line">    </span><br><span class="line">    &#x2F;&#x2F;光标坐标---------------------------------------</span><br><span class="line">void gotoxy(int x,int y)</span><br><span class="line">&#123;</span><br><span class="line">CONSOLE_SCREEN_BUFFER_INFO csbiInfo;</span><br><span class="line">HANDLE hConsoleOut;</span><br><span class="line">hConsoleOut &#x3D; GetStdHandle(STD_OUTPUT_HANDLE);</span><br><span class="line">GetConsoleScreenBufferInfo(hConsoleOut,&amp;csbiInfo);</span><br><span class="line">csbiInfo.dwCursorPosition.X &#x3D; x;</span><br><span class="line">csbiInfo.dwCursorPosition.Y &#x3D; y;</span><br><span class="line">SetConsoleCursorPosition(hConsoleOut,csbiInfo.dwCursorPosition);</span><br><span class="line">&#125;\</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">system(&quot;mode con cols&#x3D;100 lines&#x3D;34&quot;);控制台大小</span><br><span class="line"></span><br><span class="line">SetConsoleTitle(&quot;&quot;);控制台名字\</span><br><span class="line"></span><br><span class="line">&#x2F;&#x2F;隐藏控制台</span><br><span class="line">HWND hwnd;</span><br><span class="line">	hwnd&#x3D;FindWindow(&quot;ConsoleWindowClass&quot;,NULL);	&#x2F;&#x2F;处理顶级窗口的类名和窗口名称匹配指定的字符串,不搜索子窗口。</span><br><span class="line">	if(hwnd)</span><br><span class="line">	&#123;</span><br><span class="line">		ShowWindow(hwnd,SW_HIDE);				&#x2F;&#x2F;设置指定窗口的显示状态</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>
<hr>
<p>简介：</p>
<pre><code>MessageBox指的是显示一个模态对话框，其中包含一个系统图标、 一组按钮和一个简短的特定于应用程序消息，如状态或错误的信息。消息框中返回一个整数值，该值指示用户单击了哪个按钮。</code></pre><p>语法：</p>
<p><code>MessageBox( HWND hWnd, LPCTSTR lpText, LPCTSTR lpCaption, UINT uType)</code></p>
<p>参数：</p>
<p><code>hWnd</code>:</p>
<p>此参数代表消息框拥有的窗口。如果为NULL，则消息框没有拥有窗口。</p>
<p><code>lpText</code>:</p>
<p>消息框的内容。</p>
<p><code>lpCaption</code>:</p>
<p>消息框的标题。</p>
<p><code>uType</code>:</p>
<p>指定一个决定对话框的内容和行为的位标志集。此参数可以为下列标志组中标志的组合。指定下列标志中的一个来显示消息框中的按钮以及图标。</p>
<p>折叠uType参数<br>按钮:</p>
<p>按钮参数</p>
<p>含义</p>
<p><code>MB_OK</code></p>
<p>默认值。有一个确认按钮在里面。</p>
<p><code>MB_YESNO</code></p>
<p>有是和否在里面。</p>
<p><code>MB_ABORTRETRYIGNORE</code></p>
<p>有Abort(放弃)，Retry(重试)和Ignore(跳过)</p>
<p><code>MB_YESNOCANCEL</code></p>
<p>消息框含有三个按钮:Yes，No和Cancel</p>
<p><code>MB_RETRYCANCEL</code></p>
<p>有Retry(重试)和Cancel(取消)</p>
<p><code>MB_OKCANCEL</code></p>
<p>消息框含有两个按钮:OK和Cancel</p>
<p>图标:</p>
<p>参数    含义<br><code>MB_ICONEXCLAMATION</code></p>
<p>一个惊叹号出现在消息框<br><code>MB_ICONWARNING</code></p>
<p>一个惊叹号出现在消息框<br><code>MB_ICONINFORMATION</code></p>
<p>一个圆圈中小写字母i组成的图标出现在消息框<br><code>MB_ICONASTERISK</code></p>
<p>一个圆圈中小写字母i组成的图标出现在消息框<br><code>MB_ICONQUESTION</code></p>
<p>一个问题标记图标出现在消息框<br><code>MB_ICONSTOP</code></p>
<p>一个停止消息图标出现在消息框<br><code>MB_ICONERROR</code></p>
<p>一个停止消息图标出现在消息框<br><code>MB_ICONHAND</code></p>
<p>一个停止消息图标出现在消息框<br>形态:</p>
<p>参数</p>
<p>含义</p>
<p><code>MB_APPLMODAL</code></p>
<p>在hwnd参数标识的窗口中继续工作以前，用户一定响应消息框。但是，用户可以移动到其他线程的窗口且在这些窗口中工作。根据应用程序中窗口的层次机构，用户则以移动到线程内的其他窗口。所有母消息框的子窗口自动地失效，但是弹出窗口不是这样。如果既没有指定<code>MB_SYSTEMMODAL</code>也没有指定<code>MB_TASKMOOAL</code>，则<code>MB_APPLMODAL</code>为缺省的。</p>
<p><code>MB_SYSTEMMODAL</code></p>
<p>除了消息框有<code>WB_EX_TOPMOST</code>类型，<code>MB_APPLMODAL</code>和<code>MB_SYSTEMMODAL</code>一样。用系统模态消息框来改变各种各样的用户，主要的损坏错误需要立即注意(例如，内存溢出)。如果不是那些与hwnd联系的窗口，此标志对用户对窗口的相互联系没有影响。</p>
<p><code>MB_TASKMODAL</code></p>
<p>如果参数hwnd为NULL的话，那么除了所有属于当前线程高层次的窗口失效外，<code>MB_TASKMODALL</code>和<code>MB_APPLMODAL</code>一样。当调用应用程序或库没有一个可以得到的窗口句柄时，使用此标志。但仍需要阻止输入到调用线程的其他窗口，而不是搁置其他线程。</p>
<p>其他:</p>
<p>标志</p>
<p>含义</p>
<p><code>MB_DEFAULT_DESKTOP_ONLY</code></p>
<p>接收输入的当前桌面一定是一个缺省桌面。否则，函数调用失败。缺省桌面是一个在用户已经纪录且以后应用程序在此上面运行的桌面。</p>
<p><code>MB_HELP</code></p>
<p>把一个Help按钮增加到消息框。选择Help按钮或按F1产生一个Help事件。</p>
<p><code>MB_RIGHT</code></p>
<p>文本为右调整</p>
<p><code>MB_RTLREADING</code></p>
<p>用在Hebrew和Arabic系统中从右到左的顺序显示消息和大写文本。</p>
<p><code>MB_SETFOREGROUND</code></p>
<p>消息框变为前景窗口。在内部系统为消息个调用SetForegroundWindow函数。</p>
<p><code>MB_TOPMOST</code></p>
<p>消息框用<code>WS_EX_TOPMOST</code>窗口类型来创建<code>MB_SERVICE_NOTIFICATION</code>。</p>
<p>折叠返回值<br>ID</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"></span><br></pre></td></tr></table></figure>

<p>选择了……</p>
<p>IDOK(1)</p>
<p>OK</p>
<p>IDCANCEL(2)</p>
<p>CANCEL</p>
<p>IDABORT(3)</p>
<p>ABORT</p>
<p>IDRETRY(4)</p>
<p>RETRY</p>
<p>IDIGNORE(5)</p>
<p>IGNORE</p>
<p>IDYES(6)</p>
<p>YES</p>
<p>IDNO(7)</p>
<p>NO</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"></span><br></pre></td></tr></table></figure>

<p>折叠备注<br>通过将 uType 参数设置为相应的标志值，可以在消息框中系统图标，具体图标可参见MSDN网站。</p>
<p>图标</p>
<p>标志位值</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"></span><br></pre></td></tr></table></figure>


<p>MB_ICONHAND, MB_ICONSTOP, 或者MB_ICONERROR</p>
<p>MB_ICONQUESTION</p>
<p>MB_ICONEXCLAMATION 或者 MB_ICONWARNING</p>
<p>MB_ICONASTERISK 或者 MB_ICONINFORMATION</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"></span><br></pre></td></tr></table></figure>

<p>添加两个从右到左标记 (RLMs)，Unicode 格式字符中用 U+200F表示。在消息框显示字符串的开头被由<code>MessageBox</code> 渲染引擎解释，以致造成 <code>MessageBox</code> 的阅读顺序，呈现为从右向左 (RTL) 。</p>
<p>当您使用一个系统模式消息框指示时，当系统内存不足时，由 lpText 和 lpCaption 的参数的字符串不应被从资源文件，因为加载资源的尝试可能会失败。</p>
<p>如果在一个对话框存在时创建一个消息框，则使用对话框的句柄作为hWnd 参数。hWnd参数不应指定为一个子窗口，就像对话框中的控件一样。</p>
<p>实例：<br><code>MessageBox(m_hWnd , _T(&quot;这是一个最简单的消息框！&quot;));</code></p>
<p><code>MessageBox(m_hWnd，_T(&quot;这是一个有标题的消息框！&quot;),_T(&quot;标题&quot;));</code></p>
<p><code>MessageBox(m_hWnd，_T(&quot;这是一个确定 取消的消息框！&quot;),_T(&quot;标题&quot;), MB_OKCANCEL );</code></p>
<p><code>MessageBox(m_hWnd，_T(&quot;这是一个警告的消息框！&quot;),_T(&quot;标题&quot;), MB_ICONEXCLAMATION );</code></p>
<p><code>MessageBox(m_hWnd，_T(&quot;这是一个两种属性的消息框！&quot;),_T(&quot;标题&quot;), ``MB_ICONEXCLAMATION|MB_OKCANCEL );</code></p>
<p>在这里使用“_T”标注为使用Unicode，防止因为有的兼容问题而编译不过。</p>
<p>MFC特例</p>
<p>假如您创建了一个MFC工程，如果工程文档中有MainFrm.cpp的话，那么，本函数就被封装成了</p>
<p><code>int MessageBox(LPCTSTR lpszText, LPCTSTR lpszCaption = NULL,UINT nType = MB_OK);</code></p>
<p>折叠参数<br>lpszText:指向一个字符串包含要显示的消息。</p>
<p>lpszCaption:指向一个字符串包含标题。</p>
<p>nType:指定消息框的内容和行为。</p>
<p>MFC下实例：<br><code>MessageBox(_T(&quot;这是一个最简单的消息框！&quot;));</code></p>
<p><code>MessageBox(_T(&quot;这是一个有标题的消息框！&quot;),_T(&quot;标题&quot;));</code></p>
<p><code>MessageBox(_T(&quot;这是一个确定 取消的消息框！&quot;),_T(&quot;标题&quot;), MB_OKCANCEL );</code></p>
<p><code>MessageBox(_T(&quot;这是一个警告的消息框！&quot;),_T(&quot;标题&quot;), MB_ICONEXCLAMATION );</code></p>
<p><code>MessageBox(_T(&quot;这是一个两种属性的消息框！&quot;),_T(&quot;标题&quot;), MB_ICONEXCLAMATION|MB_OKCANCEL );</code></p>
<p>mfc下使用<code>MessageBox(HWND hWnd,LPCTSTR lpText, LPCTSTR lpCaption,UINT uType)</code>方法：</p>
<p><code>::MessageBox(m_hWnd，_T(&quot;这是一个两种属性的消息框！&quot;),_T(&quot;标题&quot;), MB_ICONEXCLAMATION|MB_OKCANCEL );</code></p>
<hr>
]]></content>
      <tags>
        <tag>实时更新！</tag>
      </tags>
  </entry>
</search>
